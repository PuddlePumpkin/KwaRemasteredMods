# Todo:

---

## Config Panel:
### Blueprint:
pin section header bool, scrolling keeps section header at top till reaching "section footer"
### Lua:

---

## Notifications:
### Blueprint:
### Lua:

---

## Adding new rows:

Create widget of type TypeParam

Implement on param load/update -> RMB Call Parent -> set visuals

On Construct -> Setup visuals
Implement Reconstruct

When committing a change -> Set Parameter -> Save Parameter